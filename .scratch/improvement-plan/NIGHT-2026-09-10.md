# 夜间执行计划（2026-09-10 晚）

> 前置状态：P0 全部 8 项 + P1 后端四项已完成并合入 main（CI 三 job 全绿）。
> 本计划覆盖剩余 P1 前端项 + P2 中可无人值守的测试基建项。
> 沿用 PLAN.md「0. 执行协议」全部规则：一项一 commit、commit 前过测试门禁、
> STATE.md 断点续跑、单项卡壳 30 分钟回滚跳过、02:30 硬停止。

## 今晚范围（按序执行，共 4 项核心 + 2 项有余力再做）

### 1. P2-2 前端测试体系从 0 到 1（约 60~90min）——最安全，先做

**改动清单**：
- `frontend/package.json` devDependencies 增加（版本查当日最新后 pin 死）：
  `vitest`、`@vue/test-utils`、`happy-dom`；
  scripts 增加 `"test": "vitest"`。
- 新增 `frontend/vitest.config.ts`：environment happy-dom、include `tests/**/*.spec.ts`
  （新建 frontend/tests/ 目录，不与 src 混放）、plugins 留空（纯逻辑测试不需要 vue 插件；
  若测 store 需要，加 `@vitejs/plugin-vue` 并配 alias @ → src）。
- 首批测试（纯逻辑，禁止碰 UI 组件）：
  - `frontend/tests/format.spec.ts`：formatMoney（null/负数/千分位/两位小数/非法输入→¥0.00）、
    formatDateTime（null→"-"、非法串→"-"、正常格式化）、formatDate 截取；
  - `frontend/tests/apiError.spec.ts`：getApiErrorMessage 的五个分支
    （字符串 detail / 422 数组 detail 含 loc / 无 response 网络错误 / Error 实例 / 兜底文案）、
    getApiErrorStatus（axios 403 → 403、普通 Error → null）；
    构造 fake axios error 可用 `Object.assign(new Error(), { isAxiosError: true, response: {...} })`
    （axios.isAxiosError 判定依据 isAxiosError 标记，无需真请求）；
  - `frontend/tests/authStore.spec.ts`：fetchMe 缓存 60s 内不重复请求（mock authApi.me 计数）、
    失败不缓存（第一次 reject 后第二次重新请求）、logout 清空（需 pinia：`createPinia` +
    `setActivePinia`，并 stub localStorage——happy-dom 自带）。
- `package.json` scripts 的 `test` 确认可 `npm run test -- --run` 一次性退出。
- **门禁**：`npm run build`（vue-tsc）必须仍通过 + 新测试全绿。
- commit：`test: vitest 落地——format/apiError/authStore 首批单测 [PLAN:P2-2]`

### 2. P2-4 hypothesis property tests（约 40min）

**改动清单**：
- `requirements-dev.txt` 加 `hypothesis==6.x`（pin 最新）。
- 新增 `tests/test_property_calculator.py`：
  - 任意 base_price ∈ [0.01, 999999]，`calculate_info_fee` 输出三个金额均为两位小数 Decimal、
    `deposit + balance == total_info_fee` 恒等、`total_info_fee >= DEPOSIT`；
  - base_price <= 0 时必抛 ValueError；
- 新增 `tests/test_property_state_machine.py`：
  - 遍历任意 (current, target, role) 组合：validate_transition 只可能抛 ValueError/PermissionError
    或正常返回（用 @given + settings(max_examples=200)）；
  - 废弃状态（pending_deposit/pending_approval/pending_balance）作为 target 时必抛 ValueError。
- **门禁**：全量 pytest（hypothesis 用例控制 max_examples 使总时长增加 <20s）。
- commit：`test: hypothesis property tests——精算恒等式与状态机全域合法 [PLAN:P2-4]`

### 3. P1-5 usePagedList composable（约 60min）

**改动清单**：
- 新增 `frontend/src/composables/usePagedList.ts`：
  ```ts
  export function usePagedList<T>(
    fetcher: (page: number, pageSize: number) => Promise<{ items: T[]; total?: number }>,
    opts?: { pageSize?: number }
  )
  ```
  输出 `{ items, total, loading, loadingMore, hasMore, load, loadMore, reset }`；
  内部维护 known Set 去重；hasMore = items.length < total（total 缺省时按"末页不满"推断）。
- `views/admin/OrdersList.vue` 接入：删除本地 loading/page/known 去重逻辑（约 40 行），
  保留其特有部分（URL query 同步、批量模式、编辑弹窗）。**注意**：该页 loadOrders 在 watch/statusFilter
  变化时 reset——接入后行为必须逐一对齐。
- `views/admin/FinancialRecords.vue` 接入：其 fetcher 带 filters 且响应含 summary，
  fetcher 适配为返回 items/total，summary 单独存。
- **门禁**：`npm run build` + 新增（若 P2-2 已完成）usePagedList 单测
  （mock fetcher 验证去重/hasMore/reset）。
- commit：`refactor: usePagedList composable——OrdersList/FinancialRecords 去重逻辑收敛 [PLAN:P1-5]`

### 4. P1-6 前端健壮性三件套（约 60min）

- **骨架屏**：`OrdersList.vue` 与 `ApplicationsReview.vue` 列表首屏加 `van-skeleton`
  （v-if loading 时展示 3 行占位）；`Board.vue` 推荐卡区加一行轻量占位。
- **错误边界**：`main.ts` 注册 `app.config.errorHandler`——console.error +
  `showToast("页面出现异常，请刷新重试")`（避免重复 toast：模块级节流 3s）。
- **GET 重试**：`api/client.ts` 响应拦截器中，对 `error.config.method === "get"` 且
  `!error.response`（网络层失败）且 `!error.config.__retried` 的请求：
  `__retried = true` 后延迟 400ms 重发一次（手写 Promise，不引新依赖）。
- **门禁**：`npm run build`。
- commit（可拆 2 个）：`feat: 列表骨架屏 + 全局 errorHandler [PLAN:P1-6]`、
  `feat: GET 网络失败自动重试一次 [PLAN:P1-6]`

### 5.（有余力）P1-2 剩余：ApplicationDetailDialog 拆出（约 60min）

- `views/admin/ApplicationsReview.vue` 的详情弹窗（`detailVisible`/`detailApplication` 相关
  template + 函数）整体搬到 `components/admin/ApplicationDetailDialog.vue`，
  props: `{ application: ApplicationItem | null }`，emit 审核动作回调。
- 状态文案常量已在 P0-8 完成，无需重复。
- **门禁**：`npm run build`；若 P2-2 已就位，跑一遍全量前端测试。
- commit：`refactor: ApplicationDetailDialog 从投递审核页拆出 [PLAN:P1-2]`

### 6.（有余力）P0-3 残留：存量测试迁移 conftest（每文件约 15min）

- 优先迁移 `test_final_polish.py`、`test_regressions.py`、`test_smoke.py`、`test_money_flow.py`
  到 conftest 的 db/client fixture 与造数工厂；
- **迁移必须逐文件进行、每迁一个跑一次全量 pytest**，绿了才迁下一个；
- 迁移原则：只换基建（env/DB/客户端），**不改断言语义**。
- 每文件一个 commit：`test: test_xxx 迁移 conftest fixture [PLAN:P0-3]`

## 红线（沿用 + 本次新增）

1. 沿用 PLAN.md 第 0 节全部红线（calculator 只读、不删 _ensure_*、不动 API 契约等）。
2. **不处理 Dependabot PR**（合并决定留给白天的人）；如夜间有新 PR 开出，仅记录到 STATE.md。
3. P1-1 Board.vue 拆分继续列为禁区（需真机冒烟）。
4. P1-9 Sentry、时区 A/B、P2-1 database.py 收敛：等待白天决策/外部条件，不在本计划。
5. 前端改动每项必须过 `npm run build`（含 vue-tsc）；测试迁移不得改动任何断言。

## Runbook

```
0. 读 STATE.md 确认断点 → 创建/更新勾选清单
1. P2-2 vitest          → commit → pytest + npm test + npm build
2. P2-4 hypothesis      → commit → pytest
3. P1-5 usePagedList    → commit → npm build + npm test
4. P1-6 三件套（2 commits）→ commit → npm build
5.（有余力）P1-2 弹窗拆分 → commit → npm build
6.（有余力）存量测试迁移（逐文件）
每项完成同步 STATE.md；02:30 硬停止；收尾填 PLAN.md 执行日志。
```

## 验收基线（今晚结束时应当）

- pytest ≥ 83（79 + 新增 property/单测），ruff 全绿；
- `npm run test` 退出码 0 且用例数 ≥ 10；
- `npm run build` 通过；
- OrdersList/FinancialRecords 中不再有手写分页去重样板。
